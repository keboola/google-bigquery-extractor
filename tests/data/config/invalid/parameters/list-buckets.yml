action: listBuckets
parameters:
  google:
  queries:
    - name: "US Natality statistics"
      query: "SELECT * FROM [dataSet.someTable] LIMIT 10"
      primaryKey: [ id, parentId ]
      incremental: true